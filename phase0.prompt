You are working on the ExamFlow project.

We are starting Phase 0 — Project Scaffolding.

Your task is ONLY to create the initial professional project skeleton.

IMPORTANT:

DO NOT implement any business functionality.

DO NOT implement:
- exam management
- student management
- roster import
- room management
- seat management
- seating assignment
- PDF personalization
- Crowdmark integration
- QR code processing
- signature lists
- seating maps
- audit business logic
- authentication business logic
- authorization business logic

This phase is infrastructure and project scaffolding only.

==================================================
1. READ PROJECT DOCUMENTATION FIRST
==================================================

Before making any changes, read:

- PRD.md
- ARCHITECTURE.md
- DATABASE.md
- SECURITY.md
- DEVELOPMENT.md
- AGENTS.md

Do not modify these documents unless a change is strictly required to
describe the Phase 0 scaffolding.

After reading them, briefly summarize the architectural constraints that
apply to Phase 0.

==================================================
2. PHASE 0 OBJECTIVE
==================================================

Create a clean monorepo structure for:

Frontend:
- Next.js
- React
- TypeScript
- Tailwind CSS

Backend:
- Python
- FastAPI
- Pydantic
- SQLAlchemy
- Alembic

Database:
- PostgreSQL

Development:
- Git
- Docker Compose
- environment configuration
- basic automated testing
- basic linting/type checking

The system must be able to start locally after Phase 0.

==================================================
3. REPOSITORY STRUCTURE
==================================================

Create the following structure:

examflow/
│
├── README.md
├── PRD.md
├── ARCHITECTURE.md
├── DATABASE.md
├── SECURITY.md
├── DEVELOPMENT.md
├── AGENTS.md
├── .gitignore
├── .env.example
├── docker-compose.yml
│
├── frontend/
│   ├── package.json
│   ├── tsconfig.json
│   ├── next.config.ts
│   ├── src/
│   └── ...
│
├── backend/
│   ├── pyproject.toml
│   ├── app/
│   │   ├── __init__.py
│   │   ├── main.py
│   │   ├── config.py
│   │   └── api/
│   │       └── __init__.py
│   │
│   └── tests/
│
├── docs/
│
└── scripts/

You may adjust framework-required files as necessary.

Do not create unnecessary directories.

==================================================
4. FRONTEND
==================================================

Create a minimal Next.js application using:

- Next.js
- React
- TypeScript
- Tailwind CSS

Use the current project-approved versions.

The frontend must:

- start successfully
- compile successfully
- display a simple ExamFlow placeholder page
- have no business functionality
- have no authentication implementation
- have no API business calls

The page should simply communicate that the ExamFlow application
scaffolding is running.

Example:

"ExamFlow"

"Application scaffolding is running."

Do not build a dashboard yet.

Do not create exam screens yet.

==================================================
5. BACKEND
==================================================

Create a minimal FastAPI application.

Required:

GET /health

The endpoint should return a simple response such as:

{
  "status": "ok"
}

The backend should:

- start successfully
- import successfully
- expose OpenAPI documentation
- have basic configuration loading
- have no business endpoints

Do not implement authentication.

Do not implement database business models.

==================================================
6. DATABASE
==================================================

Create the PostgreSQL development service using Docker Compose.

The database should:

- start successfully
- have configurable credentials
- use environment variables
- expose PostgreSQL on a development port if necessary

Do not create application tables yet.

Do not implement the ExamFlow database schema yet.

Alembic may be initialized, but there should be no business migrations.

If a baseline migration is required by the selected setup, keep it empty or
minimal.

==================================================
7. ENVIRONMENT CONFIGURATION
==================================================

Create:

.env.example

Include placeholders for:

DATABASE_URL
NEXT_PUBLIC_API_URL
BACKEND_PORT
FRONTEND_PORT

Do not include real credentials.

Do not commit .env.

Use environment variables consistently.

==================================================
8. DOCKER COMPOSE
==================================================

Create a minimal docker-compose.yml.

For Phase 0, PostgreSQL must be supported.

You may include frontend/backend containers only if this can be done cleanly
without making the development environment unnecessarily complicated.

Do not introduce:

- Redis
- RabbitMQ
- Kafka
- Elasticsearch
- Kubernetes
- MinIO
- additional databases

Keep Docker Compose simple.

==================================================
9. BACKEND CONFIGURATION
==================================================

Create a basic configuration system using Pydantic Settings or the
project-approved equivalent.

Configuration should support:

- application environment
- database URL
- API configuration
- CORS configuration if required for local development

Do not implement authentication configuration yet.

Do not implement Crowdmark configuration yet.

==================================================
10. DATABASE CONNECTION
==================================================

Create the basic database connection infrastructure.

It should be possible for the FastAPI application to connect to PostgreSQL.

However:

DO NOT create ExamFlow business models.

DO NOT create Student, Exam, Room, Seat, Assignment, or Document tables.

Only establish the infrastructure needed for future database development.

==================================================
11. ALEMBIC
==================================================

Initialize Alembic.

Configure it to use the application's database configuration.

Do not create business migrations.

Verify that:

alembic upgrade head

works against a fresh development database.

==================================================
12. TESTING
==================================================

Create the initial testing infrastructure.

Backend:

- pytest
- FastAPI test client

At minimum create a test for:

GET /health

Expected result:

HTTP 200

and:

{
  "status": "ok"
}

Frontend:

Set up the project's selected testing infrastructure, but do not create
business tests.

==================================================
13. CODE QUALITY
==================================================

Set up basic code quality tooling.

Backend:

- Ruff
- pytest
- optional type checking if appropriate

Frontend:

- ESLint
- Prettier if appropriate
- TypeScript type checking

The exact tool configuration should follow current project conventions.

Do not over-engineer configuration.

==================================================
14. GIT
==================================================

Create a professional .gitignore.

It must exclude at minimum:

- .env
- Python virtual environments
- __pycache__
- pytest cache
- Node modules
- Next.js build output
- IDE files
- OS-generated files
- local logs
- generated temporary files
- local databases

Do not add real credentials.

==================================================
15. README
==================================================

Update README.md with:

Project name:

ExamFlow

Short description:

A university examination administration system designed to simplify
examination preparation, seating assignment, and personalized exam document
generation.

For Phase 0, document:

- technology stack
- repository structure
- prerequisites
- local setup
- environment configuration
- how to start PostgreSQL
- how to start backend
- how to start frontend
- how to run tests
- how to run linting
- current Phase 0 limitations

Clearly state that business functionality has NOT been implemented yet.

==================================================
16. HEALTH CHECK
==================================================

The final scaffolding should support:

Frontend:

http://localhost:3000

Backend:

http://localhost:8000

Backend health:

http://localhost:8000/health

FastAPI documentation:

http://localhost:8000/docs

PostgreSQL:

localhost:5432

Use configurable ports where appropriate.

==================================================
17. DO NOT IMPLEMENT BUSINESS LOGIC
==================================================

This is extremely important.

Do NOT create:

Student model
Exam model
Room model
Seat model
ExamAssignment model
Document model
Crowdmark service
PDF generator
QR code generator
Seating algorithm
Roster importer
Signature list generator
Seating map generator

Those will be implemented in later phases.

==================================================
18. NO PREMATURE ABSTRACTIONS
==================================================

Do not create generic frameworks or abstractions simply for future
possibilities.

Do not create:

- repository framework
- service framework
- plugin system
- event bus
- message queue
- microservices
- generic CRUD generator

Create only infrastructure required by Phase 0.

==================================================
19. VALIDATION
==================================================

After implementation, actually run:

1. Backend installation
2. Backend startup
3. Frontend installation
4. Frontend startup
5. PostgreSQL startup
6. Alembic migration
7. Backend tests
8. Frontend checks
9. Backend linting
10. Type checking where configured

Do not claim a command passed unless you actually ran it.

==================================================
20. FINAL REVIEW
==================================================

Before finishing, inspect the Git diff.

Confirm:

- No business functionality was implemented.
- No real student information exists.
- No secrets exist.
- No unnecessary dependencies were added.
- No unnecessary infrastructure was added.
- No existing project documentation was incorrectly modified.
- The application starts successfully.
- PostgreSQL starts successfully.
- /health works.
- Tests pass.

==================================================
21. FINAL RESPONSE
==================================================

At the end, provide:

1. Summary of what was created.
2. Repository structure.
3. Technologies and versions selected.
4. Commands used to verify the project.
5. Test results.
6. Lint/type-check results.
7. Any known issues.
8. Any decisions that require human approval.

Do NOT proceed to Phase 1.

STOP after Phase 0 is complete.
